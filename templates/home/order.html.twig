{% extends 'base.html.twig' %}

{% block title %}Chez Marco
{% endblock %}

{% block body %}
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<a class="navbar-brand" href="{{ path('home')}}">Chez Marco</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('home')}}">Home
					</a>
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						Admin
					</a>
					<div class="dropdown-menu" aria-labelledby="navbarDropdown">
						<a class="dropdown-item" href="{{ path('menu_list')}}">List of menus</a>
						<a class="dropdown-item" href="{{ path('add_menu')}}">Add to the menu</a>
					</div>
				</li>
			</ul>
		</div>
	</nav>


    <h2 class="text-center">Your order summary</h2>

	<div class="container col-12 mt-5 d-flex flex-wrap" style="justify-content:space-between;">

		{# {{form(formAddOrder)}} #}

		



        {% for order in orderRecap %}

            <div class="card m-auto col-4 d-none">
                <div class="card-body">
                    <h5 class="card-title"></h5>
                    <h6 class="card-subtitle mb-2 text-muted" id="order_name">{{order.name}}</h6>
                    <p class="card-text" id="order_price">{{order.price}} £</p>
                    <button class="btn btn-success" onclick="">Order</button>
                </div>
            </div>

			{{ form_start(formAddOrder ,{'attr': {'id': 'formAddOrder'}} )}}

			{{ form_row(formAddOrder.content, {value : order.name}) }}
			{{ form_row(formAddOrder.totalPrice, {value : order.price}) }}
			{{ form_row(formAddOrder.Order) }}

		{{ form_end(formAddOrder) }}

        {% endfor %}


	</div>


{% endblock %}

{% block javascripts %}

<script>

		$("#add_order_content").val($("#order_name").html());

		$("#add_order_totalPrice").val($("#order_price").html());

</script>

{% endblock %}
